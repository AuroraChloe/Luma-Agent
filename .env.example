# Docker Compose database bootstrap values. Keep POSTGRES_* identical in
# backend/.env so the API container connects to this local database.
POSTGRES_DB=lumanova_agent
POSTGRES_USER=lumanova
POSTGRES_PASSWORD=replace-with-a-strong-local-password
CORE_PORT=8001
